@model SJAHE_BASE_LIBRARY.Models.PY_EmpTarifComponent

@{
    ViewBag.Title = "Create";
}

<ul class="breadcrumb">
    <li>
        <a href="@Url.Action("Index", "Home")">HOME</a>
    </li>
    <li>
        <a href="@Url.Action("Index", "EmployeeFixedComponents")">EMP TARIF COMPONENT LIST</a>
    </li>
    <li class="active">
        EMP TARIF COMPONENT CREATE
    </li>
</ul>
@using (Html.BeginForm())
{
    @Html.AntiForgeryToken()
    <div class="panel panel-pink">
        <div class="panel-heading">
            <p>
                <h4>CREATE FORM</h4>
            </p>
        </div>
        <div class="panel-body">
            <h4>EmpTarifComponent</h4>
            <hr />
            @Html.ValidationSummary(true, "", new { @class = "text-danger" })
            <div class="form-group">
                @Html.LabelFor(model => model.EmployeeNo, htmlAttributes: new { @class = "control-label" })

                @Html.DropDownList("EmpID", null, "-- SELECT EMP ID --", htmlAttributes: new { @class = "form-control" })
                @Html.ValidationMessageFor(model => model.EmployeeNo, "", new { @class = "text-danger" })

            </div>
            <div class="form-group">
                @Html.Action("ListTarifComponentPartial", "EmployeeTarifComponents")
            </div>
            <div class="form-group">
                <div class="">
                    <input type="submit" value="Create" class="btn btn-success" />
                </div>
            </div>
        </div>
    </div>
}

<div>
    @Html.ActionLink("Back to List", "Index")
</div>
